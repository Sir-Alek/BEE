# ELIA — dependencias alineadas a un entorno Python 3.10.x que ya funciona en producción.
# Python recomendado: 3.10.11 (64-bit, Windows)
#
# Nota numpy/opencv: opencv-python>=4.10 requiere numpy>=2,<2.3 en Python 3.9+.
# Por eso numpy está en 2.2.x y matplotlib en 3.10.x (soporte numpy 2.x desde 3.9.0).
#
# IMPORTANTE — dos modos de uso:
#
# 1) Ejecutable PyInstaller «delgado» (recomendado):
#    Crear un venv NUEVO y SOLO instalar este archivo. No instales TensorFlow, Torch, JAX,
#    transformers, etc. en ese venv: no los importa ELIA y solo harían el .exe enorme o
#    arrastrarían DLLs innecesarias si PyInstaller analiza ese site-packages.
#
# 2) Misma máquina donde ya tienes todo instalado:
#    Puede coincidir con tu pip freeze global; las versiones abajo deberían ser coherentes
#    entre sí. Si algo falla tras un pip install -r, resuelve conflictos priorizando las
#    versiones de ESTE archivo para los paquetes listados.
#
# Entorno virtual:
#   py -3.10 -m venv .venv
#   .venv\Scripts\activate
#   python -m pip install -U pip setuptools wheel
#   pip install -r requirements.txt
#
# tkinter: viene con el instalador oficial de Python en Windows (no es paquete pip).

# --- UI web y API local ---
fastapi==0.135.3
uvicorn==0.41.0
starlette==0.52.1
pydantic==2.12.5
# pydantic_core lo instala pip como dependencia de pydantic
httpx==0.28.1
requests==2.32.3

# --- Cifrado local (perfiles Jira / Value Edge en disco, vía API localhost) ---
cryptography==44.0.0
# Resolver explícito: selenium<4.34 acotaba typing_extensions~=4.13 (incompatible con pydantic 2.12).
typing-extensions>=4.15.0,<5

# --- Selenium / pruebas generadas ---
selenium==4.43.0
webdriver-manager==4.0.2

# --- Imagen, video, escritorio ---
Pillow==9.0.0
opencv-python==4.12.0.88
mss==10.1.0
numpy==2.2.6
PyAutoGUI==0.9.54
PyGetWindow==0.0.9

# --- Pruebas de carga API (módulo api_testing; `python -m locust`) ---
locust==2.32.0

# --- Behave y utilidades de proyectos (resources/behave, step_by_step) ---
behave==1.2.6
fpdf2==2.7.4

# --- Carga de documentos (Módulo Doc-to-BDD: Word/Excel → BDD) ---
python-multipart>=0.0.20
python-docx>=1.1.2
pandas==2.3.0
python-calamine==0.6.2
# openpyxl: fallback si calamine no puede leer el .xlsx
openpyxl>=3.1.0

# --- Grabación Móvil (Building Block: mobile_recording; licencia con flag M) ---
# Requiere además Appium Server (npm), driver uiautomator2 y adb — no son paquetes pip.
Appium-Python-Client>=4.5.0

# --- Grabación Legacy (Building Block: legacy_recording; licencia con flag L) ---
pynput>=1.8.0
pywinauto>=0.6.8
pywin32>=306                  # backend Windows para pywinauto / win32gui
comtypes>=1.4.0               # dependencia de pywinauto (UIA)

# --- Informes PDF / gráficos (PDFFeatureReport, gen_reporTest, etc.) ---
matplotlib==3.10.3

# --- Sistema ---
psutil==7.0.0

# --- IA local (opcional; «Activar IA» en conversión Behave) ---
llama-cpp-python==0.3.20

# --- Empaquetado ---
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.4

# --- Build opcional: Cython (setup_cython.py) ---
Cython==3.2.4
