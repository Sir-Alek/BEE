# Plantilla ELIA — Smoke API

Incluye un escenario HTTP de ejemplo contra JSONPlaceholder.

## Cliente API

1. Pestaña **Pruebas API** → selecciona este proyecto.
2. Abre el escenario **GET post de ejemplo** y pulsa **Enviar**.

## Behave (opcional)

En Automatización UI, plataforma **api**, ejecuta `features/api_smoke.feature`.
